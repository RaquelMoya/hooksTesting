import { MultipleCustomHooks } from "./MultipleCustomHooks";

export const Loading= ({ author, quote }) => {
    return (
        <div className="alert alert-info text-center">
            Loading...
        </div>
    )
}
